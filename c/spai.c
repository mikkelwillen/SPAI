#include "spai.h"
#include "csc.h"
#include <stdio.h>
#include <stdlib.h>

int spai(struct CSC* A, float tol, int max_fill_in, int s) {
    // initialize M and set to diagonal
    struct CSC* M = createCSC();
}
